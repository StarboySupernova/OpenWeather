////  ForecastView.swift//  OpenWeather////  Created by Simbarashe Dombodzvuku on 4/19/25.//import SwiftUIstruct ForecastView: View {    let forecast: Forecast        var body: some View {        VStack(alignment: .leading, spacing: 10) {            Text("5-Day Forecast")                .font(.title)                .fontWeight(.bold)                .padding(.top)                        ScrollView(.horizontal, showsIndicators: false) {                HStack(spacing: 15) {                    ForEach(forecast.list.prefix(12)) { item in                        DailyForecastView(weatherItem: item)                    }                }                .padding(.vertical)            }        }        .padding()        .background(Color.blue.opacity(0.1))        .cornerRadius(10)    }}struct DailyForecastView: View {    let weatherItem: WeatherList        var formattedDate: String {        let dateFormatter = DateFormatter()        dateFormatter.dateFormat = "yyyy-MM-dd HH:mm:ss"        guard let date = dateFormatter.date(from: weatherItem.dtTxt) else { return "" }                dateFormatter.dateFormat = "E, HH:mm"        return dateFormatter.string(from: date)    }        var body: some View {        VStack(alignment: .center, spacing: 8) {            Text(formattedDate)                .font(.system(size: 14))                .foregroundColor(.secondary)                        if let weather = weatherItem.weather.first {                AsyncImage(url: URL(string: "https://openweathermap.org/img/wn/\(weather.icon)@2x.png")) { image in                    image                        .resizable()                        .aspectRatio(contentMode: .fit)                        .frame(width: 50, height: 50)                } placeholder: {                    ProgressView()                        .frame(width: 50, height: 50)                }                                Text(weather.main)                    .font(.system(size: 12))                    .fontWeight(.medium)            }                        Text("\(Int(weatherItem.main.temp))°")                .font(.system(size: 18))                .fontWeight(.bold)        }        .padding(.vertical, 10)        .padding(.horizontal, 8)        .background(Color.white.opacity(0.2))        .cornerRadius(10)        .frame(width: 90)    }}